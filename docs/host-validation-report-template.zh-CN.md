# 宿主验证报告模板

## 目的

本模板用于记录 AgentSkeleton 在 `Codex` 与 `Claude Code` 中的宿主接入验证结果。

可以按“每次验证一份报告”使用，也可以在一份报告中按场景分节记录，视验证深度而定。

## 报告元信息

- 日期：
- 验证人：
- 宿主：
- 宿主接入产物：
- 被测仓库：
- AgentSkeleton 版本/提交：
- 验证场景：
- 结果：`pass` | `fail` | `partial`

## 场景摘要

- 场景名称：
- 验证目标：
- 该场景的重要性：

## 前置条件

- 运行前的仓库状态：
- 是否已经存在 AgentSkeleton 上下文：
- 相关文件或项目条件：
- 触发该场景的用户提示：

## 预期行为

- 预期宿主动作路径：
- 预期用户体验：
- 预期会使用到的稳定协议字段：

## 实际行为

- 宿主响应序列：
- 宿主选择了哪些 AgentSkeleton 动作：
- 宿主是否向用户暴露了命令名：
- 宿主是否提出了澄清问题：
- 宿主是否起草或更新了文档：
- 宿主是否 apply 了结构化答案：
- 宿主是否从 `post_apply_plan` 继续推进：
- 宿主是否刷新了过期 draft package：

## 稳定契约检查

- 是否将 `plan` 用作工作流快照：`yes` | `no` | `n/a`
- 是否在起草前使用 `focus-doc`：`yes` | `no` | `n/a`
- 是否用 `response --apply` 写回被接受的结构化结果：`yes` | `no` | `n/a`
- 是否用 `post_apply_plan` 做继续推进：`yes` | `no` | `n/a`
- 是否将 `review_candidates` 视为最新 batch 的收敛工作：`yes` | `no` | `n/a`
- 是否遵守了 `change_batch_id` 新鲜度规则：`yes` | `no` | `n/a`

## 零命令体验检查

- 用户是否必须手工选择命令：`yes` | `no`
- 用户是否必须手工查看 workflow 内部状态：`yes` | `no`
- 宿主推进是否更像任务引导而不是命令引导：`yes` | `no`
- 宿主是否只提出了必要的澄清问题：`yes` | `no`

## 结果

- 最终结果：
- 哪些部分工作正常：
- 哪些部分失败：
- 失败属于路由问题、协议问题、UX 问题还是文档问题：

## 证据

- 对话摘录：
- 产出的文件或结果：
- 相关 CLI 输出快照：
- 缺失证据说明：

## 后续动作

- 需要修复什么：
- 负责人：
- 优先级：
- 是否应阻塞 `v0.1.0`：`yes` | `no`

## 简短示例

```markdown
- 日期：2026-04-09
- 验证人：internal
- 宿主：Codex
- 宿主接入产物：skills/agentskeleton-codex/SKILL.md
- 被测仓库：sample legacy repo
- AgentSkeleton 版本/提交：e4b0d27
- 验证场景：场景 2 - 老项目重塑
- 结果：pass

## 结果

- 最终结果：pass
- 哪些部分工作正常：Codex 选择了 reshape-docs，建立了 context，执行了 plan，并提出了一个聚焦的后续问题。
- 哪些部分失败：无
- 失败属于路由问题、协议问题、UX 问题还是文档问题：n/a
```
