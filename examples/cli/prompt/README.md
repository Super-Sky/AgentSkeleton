# Prompt Examples

This directory shows how to use `agentskeleton prompt` for host-model interactions.

## Files

- `initial.yaml`: output example for `prompt --mode initial`
- `repair.yaml`: output example for `prompt --mode repair`
