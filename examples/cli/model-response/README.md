# Model Response Examples

This directory contains example host-model response envelopes.

## Included

- `valid.yaml`: a response that can be accepted into the next step
- `invalid.yaml`: a response that should trigger a repair retry
- `unresolved.yaml`: a response that should halt downstream state updates
