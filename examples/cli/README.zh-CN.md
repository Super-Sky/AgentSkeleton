# CLI Examples

这个目录用于存放 AgentSkeleton CLI 初始协议的输入输出示例。

## 当前包含

- `new-project/`：新项目 `init-docs`、`plan`、`next` 示例
- `legacy-project/`：老项目 `reshape-docs`、`plan`、`next` 示例

## 用途

这些示例可直接作为：

- 实现阶段的 fixture
- 输出结构评审材料
- CLI 与宿主模型之间的对齐基线
