# Workflow Examples

This directory shows bundled output from `agentskeleton workflow`.

## Files

- `initial.yaml`: bundled output without response file
- `with-response-accept.yaml`: bundled output with accepted response evaluation
